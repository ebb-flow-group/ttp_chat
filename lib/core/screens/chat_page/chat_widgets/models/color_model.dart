import 'package:flutter/material.dart';
import 'package:ttp_chat/config.dart';

class TextColor {
  Color textColor;
  Color bgColor;

  TextColor({this.textColor = Colors.white, this.bgColor = Config.primaryColor});
}
